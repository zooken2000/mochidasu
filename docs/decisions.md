# 決めたことと理由

後から「なぜこうなっているのか」を確かめるための記録。新しく決めたことは下に足していく。

| # | 決めたこと | 理由 | 見送った案 |
|---|---|---|---|
| 1 | 題材は「他人が書いた寄せ書き・カード」 | 本人しか持っていない一次情報で、他の応募作と重ならない | 剣道の素振り解析（参加中の別ハッカソンと題材が重なる）、オンコールAI（AWS DevOps Agent と競合） |
| 2 | 出口は「自己理解の補助」 | 人の根本は変わらない。本人が当たり前すぎて気づかない特徴を過去の言葉から教える方が、このアプリにしかできない | 面接用の自己PR文の生成（中学の寄せ書きを面接で引用するのは不自然） |
| 3 | AI は言い換えない。書くのは候補のラベルと問いかけだけ | 根拠が「AIの意見」ではなく「実在の人の言葉」であることが、キャリア系の他作品との違い | — |
| 4 | 強みの候補は2人以上が触れたものだけ（プログラムで保証） | 1人の感想では「特徴」と言えない | — |
| 5 | 雛形は Nx Plugin for AWS | Strands Agent → AgentCore → CDK の配線と、型付きクライアントの生成を一括で作れる | — |
| 6 | ログインなし（Cognito Identity Pool のゲスト ID + IAM 署名） | 審査員と AI 採点が、公開 URL からそのまま中身を試せる必要がある。AgentCore は匿名では呼べないため、ゲスト用の一時的な鍵を使う | Cognito ユーザープールでのログイン、API Gateway + Lambda を挟む構成 |
| 7 | 写真も文章も保存しない（会話は in-memory、DB なし） | 他人が書いた言葉を扱うため、「保存しない」と言える方が安心。ログインがないので保存しても持ち主を区別できない | DynamoDB への保存、S3 の会話履歴 |
| 8 | WAF・予算アラートは付けない | ハッカソン用で費用を最小にする。審査後はスタックごと消す | WAF（月 $5〜）、AWS Budgets |
| 9 | Jev（分類専用モデル）は使わない | 画面に見えない、AWS の外で審査の加点になりにくい、キー未取得、日本語の精度が不明 | Claude で読み取り → Jev で判定、の2段構成 |
| 10 | カテゴリ #personal-expression、レーン #startup | 応募作一覧で personal-expression の作品が無かった。Startup レーンでは「会社のピアボーナス・感謝ツールへ広げる」構想を語る | #community（「自分が属する集団のため」という定義に合わせにくい） |
| 11 | 最近の言葉（ピアボーナス等）の貼り付け欄を置く | 昔の紙と今の仕事の言葉がつながることを見せられる。写真なしでも試せる | ピアボーナスだけを題材にする（時間の幅が出ず、よくある HR ツールに見える） |
| 12 | 写真ごとに「紙の種類」と「何年前」を入力する | 「何年間、別々の人が」という見せ場の数字は写真から推測できない | 入力なし（AI に推測させる） |
| 13 | 開発はローカル中心（`pnpm nx dev`） | デプロイ不要で、課金は Bedrock の呼び出しだけ。デプロイは提出前に行う | — |
| 14 | ゲストの鍵は Cognito の Basic（クラシック）フローでもらう | 標準（Enhanced）フローでは未認証 ID に AWS のスコープダウンポリシーが付き、`bedrock-agentcore:InvokeAgentRuntime` が許されない（本番で 403）。Basic フローならゲスト用ロールの権限（このエージェントの呼び出しだけ）がそのまま効く | 標準フローのまま、API Gateway + Lambda を挟む |
| 15 | 使っていない `strands-agents-tools` を依存から外す | 配布物が 190MB → 108MB になり、AgentCore の起動（30秒以内）に余裕を持たせる | — |
| 16 | 配布物の起動スクリプトの先頭行を `#!/usr/bin/env python3` に書き換える（`fix_shebangs.py`） | 手元の `.venv` のパスが焼き込まれ、AgentCore で起動できなかった（CloudWatch で確認）。エントリポイントは雛形のまま `opentelemetry-instrument` を使い、トレースを残す | `python -m opentelemetry.instrumentation.auto_instrumentation`（このモジュールは `-m` で実行できない）、OTel を外す |
